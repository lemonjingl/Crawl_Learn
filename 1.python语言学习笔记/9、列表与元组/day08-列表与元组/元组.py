# ()

# 当元组里面数据只有一个的时候 数据后面需要加括号
# a = (1,)
# print(a)

# 元组是不可变数据类型 数据不能被修改
# 元组只能查找和使用 不能修改
tuple_data = (1, 2, 3)

# tuple_data # 可以进行切片和取索引操作
print(tuple_data.count(3))
print(tuple_data.index(3))
