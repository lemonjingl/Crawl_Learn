"""
按顺序输入正方形的边长（a），长方形的长（l）和宽（d），以及圆的半径（r），
计算并比较它们哪个图形面积更大，输出面积最大的图形。
"""

a = 3
l = 2
d = 3
r = 1
pi = 3.14

# lsit_data = [a * a, l * d, pi * r * r]
# max_data = max(lsit_data)
# index = lsit_data.index(max_data)
#
# dict_data = {0: '正方形', 1: '长方形', 2: '圆'}
# print(lsit_data)
# print(dict_data[index])


# lsit_data = [[a * a, '正方形'], [l * d, '长方形'], [pi * r * r, '圆']]
# lsit_data.sort(key=lambda x: x[0], reverse=True)
# print(lsit_data[0])